{
  "name": "journal-annet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run validate:snapshots && eleventy",
    "build:pages": "npm run sync:notion && npm run build",
    "dev": "eleventy --serve",
    "sync:notion": "node scripts/sync-notion.mjs",
    "test": "node --test",
    "validate:snapshots": "node scripts/validate-snapshots.mjs"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@notionhq/client": "^3.1.2"
  }
}
