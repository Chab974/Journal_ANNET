<!DOCTYPE html>
<html lang="fr" class="scroll-smooth" data-browser-title-suffix="{{ (navSection.browserTitleSuffix or "Le Journal d'Annet-sur-Marne") | escape }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <base href="{{ journal.site.pathPrefix }}">
    <title>{{ pageTitle }}</title>
    <link rel="stylesheet" href="assets/styles/tailwind.css">
    <link rel="stylesheet" href="assets/styles/site-shared.css">
    {% if pageCssHref %}
    <link rel="stylesheet" href="{{ pageCssHref }}">
    {% endif %}
